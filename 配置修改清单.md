# 博客配置修改清单

> 文件位置：`src/config.ts`
>
> 本文档记录了所有需要替换为你自己信息的配置项

---

## ✅ 修改进度追踪

- [ ] 基本信息
- [ ] 头像和图标
- [ ] 背景图片
- [ ] 个人资料
- [ ] 社交链接
- [ ] 导航栏链接
- [ ] 第三方服务配置

---

## 1. 基本信息 (第13-18行)

### 当前配置：
```typescript
title: "AcoFork Blog",
subtitle: "技术分享与实践",
description: "分享网络技术、服务器部署、内网穿透、静态网站搭建、CDN优化、容器化部署等技术教程与实践经验的个人技术博客，专注于云原生、无服务器架构和前后端开发，作者为afoim/二叉树树",
```

### 需要改成：
```typescript
title: "YeTong Blog",
subtitle: "技术探索与思维进化",
description: "分享Serverless架构、AI应用开发、认知科学、科学学习方法与前后端技术实践的个人博客，专注于云原生、无服务器计算和智能应用开发，探索技术如何赋能学习与创新。"
```

---

## 2. 头像和网站图标

### 2.1 Favicon网站图标 (第51-58行)

**当前配置：**
```typescript
favicon: [
    {
        src: "https://q2.qlogo.cn/headimg_dl?dst_uin=2726730791&spec=0",
    },
],
```

**需要改成：**
```typescript
favicon: [
    {
        src: "/favicon.png",  // 替换为你的图标路径（放在 public/ 目录下）
        // 或者使用外链：src: "https://你的域名/图标.png"
    },
],
```

**说明：**
- 如果使用本地图标，将图标文件放到 `public/` 目录
- 推荐尺寸：32x32 或 64x64 像素
- 支持格式：.png, .ico, .svg

### 2.2 个人头像 (第89-90行)

**当前配置：**
```typescript
avatar: "https://q2.qlogo.cn/headimg_dl?dst_uin=2726730791&spec=0",
```

**需要改成：**
```typescript
avatar: "/avatar.jpg",  // 本地路径（放在 public/ 目录）
// 或
avatar: "https://你的头像链接",  // 外部链接
```

**说明：**
- 推荐尺寸：200x200 像素以上
- 支持格式：.jpg, .png, .webp, .avif

---

## 3. 背景图片 (第38-46行)

### 当前配置：
```typescript
background: {
    enable: true,
    src: "https://eopfapi.2b2x.cn/pic?img=ua",  // ← 原作者的随机图片API
    position: "center",
    size: "cover",
    repeat: "no-repeat",
    attachment: "fixed",
    opacity: 0.5,
},
```

### 修改选项：

**选项A - 禁用背景：**
```typescript
background: {
    enable: false,  // ← 改为 false 即可
    // 其他配置保持不变或删除
},
```

**选项B - 使用自己的背景图：**
```typescript
background: {
    enable: true,
    src: "/background.jpg",  // 本地图片（放在 public/ 目录）
    // 或 src: "https://你的图片链接",
    position: "center",
    size: "cover",
    repeat: "no-repeat",
    attachment: "fixed",
    opacity: 0.5,  // 透明度 0-1
},
```

**选项C - 使用随机图片API：**
- Unsplash: `https://source.unsplash.com/random/1920x1080`
- Lorem Picsum: `https://picsum.photos/1920/1080`
- Bing每日图片: `https://www.bing.com/HPImageArchive.aspx?format=js&idx=0&n=1`

---

## 4. Banner横幅 (第26-37行)

### 当前配置：
```typescript
banner: {
    enable: false,  // 已禁用
    src: "/xinghui.avif",  // ← 原作者的图片
    position: "center",
    credit: {
        enable: true,
        text: "Pixiv @chokei",
        url: "https://www.pixiv.net/artworks/122782209",
    },
},
```

### 需要改成：
```typescript
banner: {
    enable: false,  // 如果不需要横幅，保持 false
    src: "/your-banner.jpg",  // 如果启用，替换为你的横幅图
    position: "center",
    credit: {
        enable: false,  // 是否显示图片来源
        text: "图片作者",
        url: "图片来源链接",
    },
},
```

**说明：**
- Banner是显示在页面顶部的大图
- 推荐尺寸：1920x400 像素
- 如果不需要可以保持 `enable: false`

---

## 5. 个人资料 (第89-105行)

### 当前配置：
```typescript
export const profileConfig: ProfileConfig = {
    avatar: "https://q2.qlogo.cn/headimg_dl?dst_uin=2726730791&spec=0",
    name: "二叉树树",
    bio: "Protect What You Love.",
    links: [
        {
            name: "Bilibli",
            icon: "fa6-brands:bilibili",
            url: "https://space.bilibili.com/325903362",
        },
        {
            name: "GitHub",
            icon: "fa6-brands:github",
            url: "https://github.com/afoim",
        },
    ],
};
```

### 需要改成：
```typescript
export const profileConfig: ProfileConfig = {
    avatar: "你的头像链接",
    name: "你的名字",
    bio: "你的个人简介",
    links: [
        {
            name: "GitHub",
            icon: "fa6-brands:github",
            url: "https://github.com/你的用户名",
        },
        {
            name: "Twitter",
            icon: "fa6-brands:twitter",
            url: "https://twitter.com/你的用户名",
        },
        // 可以添加更多社交链接
    ],
};
```

**可用的社交图标：**
- GitHub: `fa6-brands:github`
- Twitter/X: `fa6-brands:twitter` 或 `fa6-brands:x-twitter`
- Bilibili: `fa6-brands:bilibili`
- Email: `fa6-solid:envelope`
- LinkedIn: `fa6-brands:linkedin`
- YouTube: `fa6-brands:youtube`
- Instagram: `fa6-brands:instagram`
- Facebook: `fa6-brands:facebook`
- 更多图标查看：https://icon-sets.iconify.design/

---

## 6. 导航栏链接 (第61-87行)

### 当前配置：
```typescript
export const navBarConfig: NavBarConfig = {
    links: [
        LinkPreset.Home,      // 首页
        LinkPreset.Archive,   // 归档
        LinkPreset.About,     // 关于
        {
            name: "友链",
            url: "/friends/",
            external: false,
        },
        {
            name: "赞助",
            url: "/sponsors/",
            external: false,
        },
        {
            name: "统计",
            url: "https://cloud.umami.is/share/hN3l7PGcxsUCBHfU",  // ← 原作者的
            external: true,
        },
        {
            name: "状态",
            url: "https://stats.uptimerobot.com/VAtAH0mzPN",  // ← 原作者的
            external: true,
        },
    ],
};
```

### 建议修改：
```typescript
export const navBarConfig: NavBarConfig = {
    links: [
        LinkPreset.Home,
        LinkPreset.Archive,
        LinkPreset.About,
        // 删除或修改其他链接：
        // - 如果你没有"友链"和"赞助"页面，可以删除
        // - "统计"和"状态"链接改成你自己的，或删除
    ],
};
```

**修改建议：**
1. 删除 "统计" 链接（第76-80行）
2. 删除 "状态" 链接（第81-85行）
3. 如果不需要"友链"和"赞助"页面也可以删除

---

## 7. 第三方服务配置

### 7.1 图片CDN回退配置 (第113-117行)

**当前配置：**
```typescript
export const imageFallbackConfig: ImageFallbackConfig = {
    enable: true,
    originalDomain: "eo-r2.2x.nz",  // ← 原作者的CDN
    fallbackDomain: "pub-d433ca7edaa74994b3d7c40a7fd7d9ac.r2.dev",  // ← 原作者的
};
```

**建议修改：**
```typescript
export const imageFallbackConfig: ImageFallbackConfig = {
    enable: false,  // ← 如果不使用CDN回退，直接禁用
    originalDomain: "",
    fallbackDomain: "",
};
```

**说明：** 此功能用于图片CDN失败时自动切换到备用域名，如果你没有配置CDN，直接禁用即可。

### 7.2 Umami统计服务 (第119-124行)

**当前配置：**
```typescript
export const umamiConfig: UmamiConfig = {
    enable: true,
    baseUrl: "https://umami.2b2x.cn",  // ← 原作者的
    shareId: "hN3l7PGcxsUCBHfU",  // ← 原作者的
    timezone: "Asia/Shanghai",
};
```

**建议修改：**
```typescript
export const umamiConfig: UmamiConfig = {
    enable: false,  // ← 先禁用，后续配置好再启用
    baseUrl: "",
    shareId: "",
    timezone: "Asia/Shanghai",
};
```

**说明：** Umami是网站访问统计工具，需要自己部署或使用官方云服务。

### 7.3 GitHub编辑链接 (第130-133行)

**当前配置：**
```typescript
export const gitHubEditConfig: GitHubEditConfig = {
    enable: true,
    baseUrl: "https://github.com/afoim/fuwari/blob/main/src/content/posts",  // ← 原作者的
};
```

**需要改成：**
```typescript
export const gitHubEditConfig: GitHubEditConfig = {
    enable: true,
    baseUrl: "https://github.com/你的用户名/你的仓库名/blob/main/src/content/posts",
};
```

或直接禁用：
```typescript
export const gitHubEditConfig: GitHubEditConfig = {
    enable: false,  // ← 禁用GitHub编辑按钮
    baseUrl: "",
};
```

---

## 8. 其他配置

### 8.1 主题颜色 (第21-25行)

**当前配置：**
```typescript
themeColor: {
    hue: 361,           // 主色调（0-360）
    fixed: true,        // 隐藏主题颜色选择器
    forceDarkMode: true, // 强制深色模式
},
```

**颜色参考：**
- 红色：0
- 橙色：30
- 黄色：60
- 绿色：120
- 青色：180
- 蓝色：240
- 紫色：270
- 粉色：330

**根据喜好修改：**
```typescript
themeColor: {
    hue: 240,           // 改成你喜欢的颜色
    fixed: false,       // true=隐藏选择器, false=显示选择器
    forceDarkMode: false, // true=强制深色, false=可切换明暗
},
```

### 8.2 版权协议 (第107-111行)

**当前配置：**
```typescript
export const licenseConfig: LicenseConfig = {
    enable: true,
    name: "CC BY-NC-SA 4.0",
    url: "https://creativecommons.org/licenses/by-nc-sa/4.0/",
};
```

**说明：** 这是文章的版权协议声明，根据需要修改或禁用。

---

## 📝 快速修改步骤

1. ✅ 第一步：基本信息（标题、副标题、描述）
2. ✅ 第二步：头像和图标（准备好图片文件）
3. ✅ 第三步：背景图片（选择禁用或替换）
4. ✅ 第四步：个人资料（名字、简介、社交链接）
5. ✅ 第五步：导航栏（删除不需要的链接）
6. ✅ 第六步：第三方服务（禁用或配置自己的）
7. ✅ 第七步：测试运行 `pnpm dev` 查看效果

---

## 🔗 相关文件位置

- 配置文件：`src/config.ts`
- 静态资源：`public/` 目录
- 博客文章：`src/content/posts/` 目录
- 关于页面：`src/content/spec/about.md`（需要自己创建）

---

## ⚠️ 注意事项

1. 修改配置后需要重启开发服务器（`pnpm dev`）
2. 图片文件放在 `public/` 目录下，引用时使用 `/文件名`
3. 外部链接必须以 `http://` 或 `https://` 开头
4. 修改前建议备份 `src/config.ts` 文件

---

**修改完成后运行：**
```bash
pnpm dev
```

在浏览器中查看效果：`http://localhost:4321`
